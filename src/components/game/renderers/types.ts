// Re-export the GameRenderer interface as the canonical import for renderer implementations
export type { GameRenderer } from '../types';
export type { Vec2, Camera } from '../types';
export { GAME_CONSTANTS } from '../types';
