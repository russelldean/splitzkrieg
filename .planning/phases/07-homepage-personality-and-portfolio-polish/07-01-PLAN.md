---
phase: 07-homepage-personality-and-portfolio-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/icons.tsx
  - src/components/layout/Header.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-PERSONALITY
must_haves:
  truths:
    - "Homepage card grid shows icon + label only, no description text"
    - "Homepage uses custom SVG icons instead of emoji"
    - "Homepage tagline has specific voice, not generic stats description"
    - "Card grid layout breaks the uniform 2x3/6-col grid pattern"
    - "SVG icons are importable from a shared location"
    - "Header still renders correctly after icon extraction"
  artifacts:
    - path: "src/components/ui/icons.tsx"
      provides: "Shared SVG icon exports for nav and homepage"
      exports: ["leagueNightsIcon", "seasonsIcon", "statsIcon", "bowlersIcon", "teamsIcon", "blogIcon"]
    - path: "src/app/page.tsx"
      provides: "Redesigned homepage with personality"
      contains: "quickLinks"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/components/ui/icons.tsx"
      via: "named imports"
      pattern: "import.*from.*icons"
    - from: "src/app/page.tsx"
      to: "src/components/ui/icons.tsx"
      via: "named imports"
      pattern: "import.*from.*icons"
---

<objective>
Strip the AI-generated feel from the homepage by extracting shared SVG icons, replacing the uniform card grid with a compact pill/chip row (no descriptions, SVG icons), and rewriting the generic tagline with league-specific voice.

Purpose: The homepage is the #1 "looks AI-made" offender per league member feedback. The uniform 6-card grid with emoji + descriptions is the primary tell. This plan fixes the homepage personality while extracting icons for reuse.

Output: Shared icon file, redesigned homepage quick-links, opinionated tagline
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-homepage-personality-and-portfolio-polish/07-CONTEXT.md
@.planning/phases/07-homepage-personality-and-portfolio-polish/07-RESEARCH.md

@src/components/layout/Header.tsx
@src/app/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/layout/Header.tsx (lines 7-52):
Six SVG icon constants defined as JSX: bowlersIcon, teamsIcon, seasonsIcon, leagueNightsIcon, blogIcon, statsIcon.
Each is an `<svg viewBox="0 0 20 20" fill="currentColor" className="w-4 h-4">` with custom paths.

From src/app/page.tsx (lines 28-35):
```typescript
const quickLinks = [
  { label: 'League Nights', href: '/week', description: 'Weekly matchups and scores', icon: '🎳', accent: 'border-l-red-600/40' },
  { label: 'Seasons', href: '/seasons', description: 'Every season since 2007', icon: '📅', accent: 'border-l-navy/30' },
  // ... 4 more with same structure
];
```

From src/components/ui/ParallaxBg.tsx:
```typescript
interface ParallaxBgProps {
  src: string;
  focalY?: number;
  imgW?: number;
  imgH?: number;
  mobileSrc?: string;
  mobileFocalY?: number;
  mobileImgW?: number;
  mobileImgH?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SVG icons to shared file and update Header imports</name>
  <files>src/components/ui/icons.tsx, src/components/layout/Header.tsx</files>
  <action>
Create `src/components/ui/icons.tsx` with named exports for all 6 SVG icons currently defined as constants in Header.tsx (lines 7-52): `leagueNightsIcon`, `seasonsIcon`, `statsIcon`, `bowlersIcon`, `teamsIcon`, `blogIcon`.

Each icon should be exported as a React JSX element (not a component function), preserving the exact SVG markup and `className="w-4 h-4"` from Header.tsx. Keep `viewBox="0 0 20 20"` and `fill="currentColor"`.

Then update Header.tsx:
- Remove the 6 `const xxxIcon = (...)` blocks (lines 7-52)
- Add `import { bowlersIcon, teamsIcon, seasonsIcon, leagueNightsIcon, blogIcon, statsIcon } from '@/components/ui/icons';` at the top
- The rest of Header.tsx remains unchanged -- it already references these icons by variable name

Do NOT modify any SVG paths or dimensions. This is a pure extraction refactor.
  </action>
  <verify>
    <automated>cd /Users/russdean/Projects/splitzkrieg && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 6 icons exported from icons.tsx, Header.tsx imports them, TypeScript compiles clean, header renders identically</done>
</task>

<task type="auto">
  <name>Task 2: Redesign homepage card grid and tagline</name>
  <files>src/app/page.tsx</files>
  <action>
Modify `src/app/page.tsx` to fix the three main AI-pattern issues:

**1. Import shared icons:**
Add `import { leagueNightsIcon, seasonsIcon, statsIcon, bowlersIcon, teamsIcon, blogIcon } from '@/components/ui/icons';`

**2. Replace quickLinks array (lines 28-35):**
Remove `description` and `icon` (emoji) fields. Add SVG icon references instead:
```
{ label: 'League Nights', href: '/week', icon: leagueNightsIcon, accent: 'border-l-red-600/40' },
{ label: 'Seasons', href: '/seasons', icon: seasonsIcon, accent: 'border-l-navy/30' },
{ label: 'The Stats', href: '/stats', icon: statsIcon, accent: 'border-l-red-600/40' },
{ label: 'Bowlers', href: '/bowlers?filter=current', icon: bowlersIcon, accent: 'border-l-navy/30' },
{ label: 'Teams', href: '/teams?filter=current', icon: teamsIcon, accent: 'border-l-red-600/40' },
{ label: 'Blog', href: '/blog', icon: blogIcon, accent: 'border-l-navy/30' },
```

**3. Replace the card grid markup (lines 147-165):**
Change from `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6` to a flex-wrap inline pill/chip row:
```tsx
<div className="flex flex-wrap gap-2 mt-6 sm:mt-8">
  {quickLinks.map((link) => (
    <Link
      key={link.href}
      href={link.href}
      className={`inline-flex items-center gap-2 bg-white rounded-lg border border-navy/10 border-l-4 ${link.accent} px-4 py-2.5 hover:border-navy/20 hover:shadow-sm transition-all group`}
    >
      <span className="text-navy/50 group-hover:text-red transition-colors">{link.icon}</span>
      <span className="font-body text-sm font-medium text-navy group-hover:text-red transition-colors">
        {link.label}
      </span>
    </Link>
  ))}
</div>
```
Key differences from current: no descriptions, flex-wrap instead of grid, pills instead of cards, SVG icons instead of emoji. Keep the alternating red/navy border-l accent per locked decision.

**4. Replace the generic tagline (line 108-109):**
Change `Stats, records, and {years} years of league history` to something with voice. Draft 3 options, pick the most genuine:
- "Durham's longest-running bowling league, since 2007"
- "19 years of gutters, strikes, and questionable team names"
- "Since 2007. {totalBowlers}+ bowlers. One very specific website."

Pick whichever feels most like "Baseball Reference with a wink" per the tone guidance. Lean toward dry/understated, not trying too hard. Keep it short.

Also update the metadata description (line 25) to match the new tagline tone.

**5. Remove the gradient divider (lines 169-172):**
The gradient divider between hero and content grid is another template pattern. Remove it entirely -- the visual break between the pill row and the content grid provides enough separation.

Do NOT modify the Milestone Ticker, Results CTA, CountdownClock, MiniStandings, ThisWeekMatchups, or SeasonSnapshot sections. Those are working well.
  </action>
  <verify>
    <automated>cd /Users/russdean/Projects/splitzkrieg && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Homepage shows compact pill-style nav links with SVG icons and no descriptions, tagline has league-specific voice, gradient divider removed, all other homepage sections unchanged</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `next dev` renders homepage with pill-style quick links (no grid, no descriptions)
- Header SVG icons render identically to before (extraction was pure refactor)
- Homepage tagline is specific to Splitzkrieg, not generic
- Alternating red/navy left border accents preserved on pills
</verification>

<success_criteria>
- Homepage quick-link cards are compact pills with SVG icons + label only (no descriptions)
- Layout is flex-wrap row, not a uniform grid
- Tagline references the league specifically, not generic stats language
- Gradient divider between hero and content is gone
- Header navigation icons unchanged in appearance
- No new npm dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/07-homepage-personality-and-portfolio-polish/07-01-SUMMARY.md`
</output>
