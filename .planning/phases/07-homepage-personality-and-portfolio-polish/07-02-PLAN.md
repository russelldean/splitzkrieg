---
phase: 07-homepage-personality-and-portfolio-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/bowlers/page.tsx
  - src/app/teams/page.tsx
  - src/app/seasons/page.tsx
  - src/components/season/SeasonDirectory.tsx
autonomous: false
requirements:
  - DIR-HEROES
  - AI-AUDIT
must_haves:
  truths:
    - "Bowlers directory page has a parallax photo hero header"
    - "Teams directory page has a parallax photo hero header"
    - "Seasons directory page has a parallax photo hero header"
    - "Directory heroes show page title and contextual count"
    - "No page in scope triggers AI-made pattern recognition"
    - "Someone unfamiliar with the league understands what it is within 5 seconds of landing"
  artifacts:
    - path: "src/app/bowlers/page.tsx"
      provides: "Bowler directory with parallax hero"
      contains: "ParallaxBg"
    - path: "src/app/teams/page.tsx"
      provides: "Teams directory with parallax hero"
      contains: "ParallaxBg"
    - path: "src/app/seasons/page.tsx"
      provides: "Seasons directory with parallax hero"
      contains: "ParallaxBg"
  key_links:
    - from: "src/app/bowlers/page.tsx"
      to: "src/components/ui/ParallaxBg.tsx"
      via: "import"
      pattern: "import.*ParallaxBg"
    - from: "src/app/teams/page.tsx"
      to: "src/components/ui/ParallaxBg.tsx"
      via: "import"
      pattern: "import.*ParallaxBg"
    - from: "src/app/seasons/page.tsx"
      to: "src/components/ui/ParallaxBg.tsx"
      via: "import"
      pattern: "import.*ParallaxBg"
---

<objective>
Add parallax photo hero headers to all three directory pages (bowlers, teams, seasons) using Village Lanes photos, and perform a final AI-pattern audit across the homepage + directories. Ends with a visual checkpoint for user approval.

Purpose: Directory pages feel thin and clinical without visual anchoring. The parallax hero treatment has been the standout visual element on the site -- extending it to directories adds warmth and community feeling. The final audit ensures no remaining AI-pattern triggers.

Output: Three directory pages with parallax heroes, AI-audit pass, user visual approval
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-homepage-personality-and-portfolio-polish/07-CONTEXT.md
@.planning/phases/07-homepage-personality-and-portfolio-polish/07-RESEARCH.md
@.planning/phases/07-homepage-personality-and-portfolio-polish/07-01-SUMMARY.md

@src/app/bowlers/page.tsx
@src/app/teams/page.tsx
@src/app/seasons/page.tsx
@src/components/season/SeasonDirectory.tsx
@src/components/bowlers/BowlerDirectory.tsx
@src/components/team/TeamsDirectory.tsx
@src/components/ui/ParallaxBg.tsx

<interfaces>
<!-- ParallaxBg component interface -->
From src/components/ui/ParallaxBg.tsx:
```typescript
interface ParallaxBgProps {
  src: string;
  focalY?: number;       // 0=top, 1=bottom, default 0.65
  imgW?: number;         // native width, default 640
  imgH?: number;         // native height, default 478
  mobileSrc?: string;
  mobileFocalY?: number;
  mobileImgW?: number;
  mobileImgH?: number;
}
```

Usage pattern (from page.tsx):
```tsx
<div className="relative overflow-hidden h-40">
  <ParallaxBg src="/village-lanes-chairs.jpg" imgW={2048} imgH={1536} focalY={0.5}
    mobileSrc="/village-lanes-lanes.jpg" mobileFocalY={0.6} mobileImgW={3024} mobileImgH={4032} />
  <div className="absolute inset-0 bg-black/60" />
  <div className="relative z-10">Content on top</div>
</div>
```

From BowlerDirectory component:
- Receives `bowlers: DirectoryBowler[]`
- Renders heading with count inside itself (h1 + count paragraph)
- Has Current/All toggle filter

From TeamsDirectory component:
- Receives `teams: DirectoryTeam[]`
- Renders heading inside itself (h1)
- Has Current/All toggle filter

From SeasonDirectory component:
- Server component, renders its own `<main>` wrapper with max-w-3xl
- Props: seasons, currentSlug, trailCurrent, heading, subheading(count)
- ALSO used by src/app/stats/page.tsx -- so heading removal must be conditional
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parallax hero headers to all three directory pages</name>
  <files>src/app/bowlers/page.tsx, src/app/teams/page.tsx, src/app/seasons/page.tsx, src/components/season/SeasonDirectory.tsx</files>
  <action>
Add a parallax photo hero header to each of the three directory pages. The hero sits above the existing content and includes the page title + a contextual subtitle with count data.

**Photo assignments (per research recommendations):**
- Bowlers: `village-lanes-panorama.jpg` (wide lanes shot, community feel)
- Teams: `village-lanes-group-photo.jpg` (group/team energy)
- Seasons: `village-lanes-bowl-sign.jpg` (venue identity, history/legacy)

**Hero pattern for each page:**
```tsx
<section className="relative overflow-hidden h-36 sm:h-44">
  <ParallaxBg
    src="/village-lanes-{photo}.jpg"
    imgW={...} imgH={...}
    focalY={0.4}
    mobileSrc="/village-lanes-{photo}.jpg"
    mobileFocalY={0.5}
    mobileImgW={...} mobileImgH={...}
  />
  <div className="absolute inset-0 bg-gradient-to-r from-navy/80 to-navy/50" />
  <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-full flex items-end pb-6">
    <div>
      <h1 className="font-heading text-3xl sm:text-4xl text-white">{title}</h1>
      <p className="font-body text-white/70 text-sm mt-1">{subtitle with count}</p>
    </div>
  </div>
</section>
```

Use `aria-hidden="true"` on the ParallaxBg or add meaningful `role="img"` + `aria-label` on the hero section for accessibility.

**Bowlers page (`src/app/bowlers/page.tsx`):**
- Add ParallaxBg import
- Insert hero section before the `max-w-7xl` content div
- Hero title: "Bowlers"
- Hero subtitle: use `bowlers.length` for count -- e.g., "{count} bowlers across 19 years of Splitzkrieg history"
- The BowlerDirectory component currently renders its own h1 heading. Since the hero now provides the h1, the BowlerDirectory component's heading will be visually redundant. However, do NOT modify BowlerDirectory.tsx (it's a client component with filter state). Instead, the hero provides the page-level heading and BowlerDirectory's internal heading serves as the content section heading -- this is fine since BowlerDirectory's h1 changes between "Current Bowlers" and "All Bowlers" based on filter, which is useful context below the hero.

**Teams page (`src/app/teams/page.tsx`):**
- Add ParallaxBg import
- Insert hero section before the `max-w-5xl` content area
- Hero title: "Teams"
- Hero subtitle: use `teams.length` for count -- e.g., "{count} franchises in Splitzkrieg history"
- Same approach as bowlers -- TeamsDirectory's internal h1 changes with filter, keep it.

**Seasons page (`src/app/seasons/page.tsx`):**
- This page delegates to SeasonDirectory component which renders its own `<main>` tag
- Wrap in a fragment, add hero section above SeasonDirectory. Pass `seasons.length` for count.
- Hero title: "Seasons"
- Hero subtitle: "{count} seasons of Splitzkrieg bowling"
- SeasonDirectory renders its own h1 ("Seasons"). Since stats/page.tsx also uses SeasonDirectory, add an optional `hideHeading?: boolean` prop to SeasonDirectory. Pass `hideHeading={true}` from seasons/page.tsx. The stats page continues to show its heading unchanged.
- In SeasonDirectory.tsx: when `hideHeading` is true, skip rendering the h1 and subheading p tag (lines 28-31). Keep TrailNav and all content below.

For image dimensions, check each photo with `identify` or use reasonable estimates:
- panorama: imgW={4032} imgH={3024}
- group-photo: imgW={4032} imgH={3024}
- bowl-sign: imgW={4032} imgH={3024}

Tune focalY values to show the interesting part of each photo. Start with focalY={0.4} for all and adjust if needed during dev server testing.
  </action>
  <verify>
    <automated>cd /Users/russdean/Projects/splitzkrieg && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three directory pages show parallax photo hero headers with page title and contextual count, no duplicate headings on seasons page, stats page heading unchanged, TypeScript compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual review of homepage personality and directory heroes</name>
  <files>src/app/page.tsx, src/app/bowlers/page.tsx, src/app/teams/page.tsx, src/app/seasons/page.tsx</files>
  <action>
Present the completed changes to the user for visual review. Start the dev server if not running and provide URLs for each page to check.
  </action>
  <verify>
    <automated>cd /Users/russdean/Projects/splitzkrieg && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User approves visual changes or provides specific feedback for iteration</done>
  <what-built>
Homepage personality overhaul (Plan 01) and directory page hero headers (this plan):
- Homepage: pill-style quick links with SVG icons, no descriptions, new tagline
- Bowlers page: parallax hero with panorama photo
- Teams page: parallax hero with group photo
- Seasons page: parallax hero with bowl sign photo
  </what-built>
  <how-to-verify>
1. Run `next dev` and visit http://localhost:3000
2. **Homepage check:**
   - Quick links should be compact pills in a flex-wrap row (not a 6-card grid)
   - Each pill shows SVG icon + label only (no description text)
   - Alternating red/navy left border accents on pills
   - Tagline below logo has specific league voice, not generic
   - No gradient divider between hero and content grid
   - Does it still feel AI-generated? The card grid was the main offender.
3. **Bowlers page** (http://localhost:3000/bowlers):
   - Parallax photo hero at top (Village Lanes panorama)
   - White text title + subtitle on top of dark overlay
   - Photo shows interesting part (not cropped badly)
   - Existing filter + alphabetical list below unchanged
4. **Teams page** (http://localhost:3000/teams):
   - Parallax photo hero at top (group photo)
   - Card grid below unchanged
5. **Seasons page** (http://localhost:3000/seasons):
   - Parallax photo hero at top (bowl sign)
   - Featured current season card below unchanged
6. **Mobile check:** Resize browser to mobile width, verify heroes don't break layout
7. **Overall:** Does the site feel "made" rather than "generated"?
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 7, or describe specific issues to address</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All three directory pages render parallax hero headers with Village Lanes photos
- Homepage quick links are pills with SVG icons (from Plan 01)
- No duplicate h1 headings on seasons page
- Mobile layout works for all modified pages
- User confirms visual approval at checkpoint
</verification>

<success_criteria>
- All three directory pages have parallax photo hero headers
- Heroes show page title + contextual subtitle with data counts
- No remaining "AI-made" pattern triggers on homepage or directory pages
- User approves the visual changes at checkpoint
- Inner pages (bowler profiles, team pages, season detail pages) are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/07-homepage-personality-and-portfolio-polish/07-02-SUMMARY.md`
</output>
